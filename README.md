### Small Python Project Collection 🐍
This repository contains a variety of small, practical Python projects demonstrating the use of different libraries and concepts. Each folder contains a self-contained script or module for a specific task.

**Fetch_Weather**: A simple application to retrieve weather information for a given location, likely using libraries like ```requests``` to interact with weather APIs.

**Linux_Command_scraper**: A tool to extract Linux commands from web pages or documentation, possibly employing web scraping libraries such as ```BeautifulSoup``` and ```requests```, or using regular expressions (```re```).

**StonePaperScissor**: A classic console-based game implementing the Rock-Paper-Scissors logic, utilizing Python's built-in ```random``` module for opponent choices.

**stright_distance**: A module for calculating the straight-line distance between points, likely using mathematical functions from the ```math``` library.
